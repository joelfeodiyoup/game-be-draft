## why mongodb?

- document storage for game state, allowing the user to resume
- in a real system I'd quite possibly use postgres jsonb instead, to reduce complexity of deployment. But for this demo I just want to use mongodb.